/3code
/3code.nimcache/
/3code.pending
/3code.build-lock/
3code.exe
0.5/
bin/
.cache/
timing/
build/
# testament drops compiled test binaries next to their source (no extension);
# test sources (.nim/.nims) are negated back in, as are the scripts under
# tests/tools and the fixtures under tests/testdata
tests/*/*
!tests/*/*.nim
!tests/*/*.nims
!tests/tools/*
!tests/testdata/**
tests/testdata/output/
testresults/

# Compiled probe binaries next to their .nim sources. Sources are tracked
# by virtue of having a .nim extension; the bare binary (no extension) is
# build output and contains the API key inlined by the source.
/tests/testdata/probe_minimax_sse
/tests/testdata/probe_minimax_split
nimcache/
config.local.nims

.archive/
.agents/
src/threecode.out
tests/tools/pty_frames
tests/golden/*.actual
nimble.develop
nimble.paths
nimbledeps
src/threecode.exe

# stress runner binary
tests/tools/stress

# compiled test binaries at tests/ root level
tests/megatest
knowngood_table
AGENTS.md
