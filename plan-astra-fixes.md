# Astra fixes

Implement the findings in docs/astra-review.md, requested after review commit a8ae607. Preserve unrelated existing plans and user work. Commit coherent verified chunks; no installs or pushes. Use fresh contexts between stages. This plan is the durable handoff, update it after each stage.

## Stages

- [x] 1. Trustworthy assertions and test execution. Reproduce early-exit expect false pass in a permanent harness regression, split waiting from asserting where appropriate, ensure assertion helpers always fail on unmet expectations. Consolidate local/CI runner selection and failure aggregation, honor testament metadata, quote arguments, scope watchdog kills to owned descendants, fix POSIX arithmetic. Add negative runner tests. Inspect tests and existing notes first.
- [ ] 2. Build provenance. Consolidate tests/stub_helpers.nim duplicated builders. Let Nim's incremental compiler track source/include/staticRead/dependency inputs rather than partial mtimes; serialize shared builds and safely publish artifacts. Avoid invoking dependency resolution per test unnecessarily; respect nimble.paths and plain nim c. Ensure production executable preparation uses same reliable strategy. Tests for edited stub inputs/config/defines and concurrent cold builds as practical.
- [ ] 3. Session and transport identity. Make new session identities collision resistant with atomic resource allocation, preserving old lookups and session-format expectations. Key locks by actual resource identity, not timestamp basename. Add simultaneous independent-root and same-root tests. Include TLS/plain mode in connection cache identity and centralize repeated connection acquisition without combining protocol-specific decoders. Clarify loopback HTTP as test capability or explicit supported product capability. Reproduce scheme-switch issue in local mock transport test, no external credentials. Inspect library global ownership boundaries and address concrete unsafe concurrent use without speculative framework.
- [ ] 4. Lossless visual harness. Read .agents/design.md, .agents/development-guide.md, and root notes first; .agents/testing.md and .agents/osx-testing.md were missing and must be restored using current evidence. Add harness regression tests BEFORE changes for cursor-only moves/visibility, wide and combining character overlay, style differences, and wrap normalization. Preserve structured cell attributes and explicit cursor state; stable frame/checkpoint identities; geometry-preserving redaction; one artifact format accepted by comparator/viewer; first difference coordinates and useful crops. Do not blindly regenerate golden output. Real emulator screenshots required where ttty cannot reproduce disagreement; state verification surfaces.
- [ ] 5. Deterministic scenarios and docs. Use monotonic deadlines, explicit readiness events rather than silence, report quiet-cap expiry. Keep raw capture continuous independently from selected comparison checkpoints. Make broad shakedown scenarios individually selectable without exploding executables; isolate demonstrated unsupported scenarios rather than blanket disabling platforms where possible. Separate stress lane, expose timing/build identity and one run/diff/view workflow. Restore canonical docs, archive historical guidance clearly rather than rewriting unrelated plans. Split prompts.nim responsibilities only where it yields a small coherent improvement; avoid broad speculative framework. Add examples and end-to-end tests for new user-facing tooling per project notes.
- [ ] 6. Integration. Review all findings against fixes, run focused and broader suite, debug failures rather than weaken expectations. Run release compile with plain nim c, no nimble install. Record actual timing and verification surface, platform limitations, and any remaining risks in docs/astra-review.md. All local actionable work completed before final response.

## Current state

Baseline a8ae607, clean tree before this plan. Review changed GPT prompt and prompt integration tests; 84 tests passed via `nim c -r --out:/tmp/astreview-prompts tests/api/test_prompts_compact.nim`. `nimble setup` already generated local nimble.paths with develop-linked ttty/streamhttp/sandwall/tinotify. No package installs. Temporary /tmp/astreview_expect_probe.nim reproduced false pass: /bin/true under newTtySession, expectExit(0), expect nonexistent text continues successfully. Its compile command needed explicit --path:/home/carlo/p/ttty/src because /tmp source does not inherit repo config. Repo-root tests inherit config correctly.

Relevant known evidence:
- tests/tty_expect.nim expect around 1440 returns false on exited child, discardable.
- rememberFrame around 178 dedups text only; normalizeFrameRows around 1239 removes end cursor; frameRowsWithCursor counts runes not widths; normalizeWrappedPathTail around 1391 merges rows.
- tests/stub_helpers.nim ensureStubBinary scans only src mtimes; buildBinary additionally scans stub. Both miss config/staticRead/dependencies and no locking.
- threecode.nimble test task joins commands with semicolons, fallback ignores testament specs.
- tools/ci_tests.sh global ps/pkill patterns can hit unrelated runs; 10# arithmetic non-POSIX (local sh supports it, dash unavailable).
- src/threecode/session.nim newSessionPath second granularity, shared temp lock keyed basename.
- src/threecode/api.nim four acquisition paths cache host:port without scheme; config.nims globally defines testPlainHttp (loopback only).
- tools/pty_frames.nim parses raw frame headers only, not meaningful golden headers.
- tests/tty/test_tty_functional.nim 46 tests/3102 lines, disabled osx/win file-wide.

## Stage 1 completed

Permanent PTY contract regression reproduced four exited-child false passes (expect, expectOnScreen, expectCount, expectNo) before fixes. Added waitForText boolean query; expect asserts consistently and distinguishes exit/deadline. Fixed other false-return assertions and negative check after exit. Regression includes stale history count, absence, live deadline, wrong exit, dead child, caret/prompt/history assertions. Surface: POSIX PTY interpreted by ttty, no rendering implementation changes or emulator claim.

Shared tools/test_dispatch.sh now selects all/files/categories through testament only, quotes literal arguments, aggregates failures. nimble task uses it and plain Nim incremental build (serialization comes in stage 2). CI uses same dispatcher. Watchdog descendants/etime functions isolated in tools/test_processes.sh, no global process-name kills or delayed PID kill. Signal cleanup scoped to owned tree. Portable awk decimal parsing replaces 10#. Recursive descendants uses subshell function to avoid POSIX global-variable recursion corruption.

Validation: plain nim c -r for tests/core/test_runner.nim and tests/tty/test_expect_contract.nim, then shared dispatch via real testament: 2/2 files pass (2.49s + 2.77s). Runner fake-testament tests fail-first/pass-last files/categories, literal metacharacter arguments, missing testament, elapsed 08/09/day parsing, owned subtree kill while unrelated sleep survives. nimble tasks parses successfully. git diff --check passes. No dash installed, macOS/Windows not run. Full CI watchdog timeout integration remains useful in stage 6; unit tree ownership tested. Root/development notes read; no nested AGENTS under tests/tools/.agents.

Next: stage 2 build provenance. Read tests/stub_helpers.nim and consumers, consolidate builders with serialized incremental compilation and atomic publication including production preparation; permanent regressions. Commit coherent stage and continue fresh handoffs. User explicitly authorized fixing everything in the review, not only making another plan.
