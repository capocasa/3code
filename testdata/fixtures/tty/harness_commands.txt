===== 989 =====

  ╭─╮
   ─┤  3code v0.6.0-sysmodel-5df6f76d   the economical coding agent
  ╰─╯

  provider  stub
  model     stub-model

  type a prompt. :help for commands. :q or Ctrl-D to exit.

❯ :help
===== 351 =====
  :provider add     add a new provider (interactive, verified)
  :provider edit X  edit provider X (url, key, models)
  :provider rm X    remove provider X
  :reasoning        list reasoning levels for current model (* marks active)
  :reasoning X      switch reasoning level (low / medium / high)
  :streaming        show streaming mode (on = live output, off = request/response)
  :streaming on|off toggle SSE streaming (off is the reliable fallback for flaky SSE)
  :notify           show notify mode (on = desktop notification when a turn ends)
  :notify on|off    toggle turn-end desktop notification
  :retry            show patient-retry mode (on = ~36h hold for outages/limits)
  :retry on|off     toggle patient retry of 429/5xx/network errors
  :prompt           show the active system prompt
  :show [N]         show full output of tool call N (default: last)
  :log              list all tool calls this session
  :sessions         list recent sessions saved in this directory (max 20)
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
❯
===== 323 =====
  :provider add     add a new provider (interactive, verified)
  :provider edit X  edit provider X (url, key, models)
  :provider rm X    remove provider X
  :reasoning        list reasoning levels for current model (* marks active)
  :reasoning X      switch reasoning level (low / medium / high)
  :streaming        show streaming mode (on = live output, off = request/response)
  :streaming on|off toggle SSE streaming (off is the reliable fallback for flaky SSE)
  :notify           show notify mode (on = desktop notification when a turn ends)
  :notify on|off    toggle turn-end desktop notification
  :retry            show patient-retry mode (on = ~36h hold for outages/limits)
  :retry on|off     toggle patient retry of 429/5xx/network errors
  :prompt           show the active system prompt
  :show [N]         show full output of tool call N (default: last)
  :log              list all tool calls this session
  :sessions         list recent sessions saved in this directory (max 20)
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
❯ :provider
===== 937 =====
  :streaming        show streaming mode (on = live output, off = request/response)
  :streaming on|off toggle SSE streaming (off is the reliable fallback for flaky SSE)
  :notify           show notify mode (on = desktop notification when a turn ends)
  :notify on|off    toggle turn-end desktop notification
  :retry            show patient-retry mode (on = ~36h hold for outages/limits)
  :retry on|off     toggle patient retry of 429/5xx/network errors
  :prompt           show the active system prompt
  :show [N]         show full output of tool call N (default: last)
  :log              list all tool calls this session
  :sessions         list recent sessions saved in this directory (max 20)
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
❯
===== 111 =====
  :streaming        show streaming mode (on = live output, off = request/response)
  :streaming on|off toggle SSE streaming (off is the reliable fallback for flaky SSE)
  :notify           show notify mode (on = desktop notification when a turn ends)
  :notify on|off    toggle turn-end desktop notification
  :retry            show patient-retry mode (on = ~36h hold for outages/limits)
  :retry on|off     toggle patient retry of 429/5xx/network errors
  :prompt           show the active system prompt
  :show [N]         show full output of tool call N (default: last)
  :log              list all tool calls this session
  :sessions         list recent sessions saved in this directory (max 20)
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
❯ :model
===== 752 =====
  :retry on|off     toggle patient retry of 429/5xx/network errors
  :prompt           show the active system prompt
  :show [N]         show full output of tool call N (default: last)
  :log              list all tool calls this session
  :sessions         list recent sessions saved in this directory (max 20)
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
❯
===== 890 =====
  :retry on|off     toggle patient retry of 429/5xx/network errors
  :prompt           show the active system prompt
  :show [N]         show full output of tool call N (default: last)
  :log              list all tool calls this session
  :sessions         list recent sessions saved in this directory (max 20)
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
❯ :reasoning
===== 229 =====
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
❯
===== 240 =====
  :summarize        collapse old turns into a synthetic recap (meta model call)
  :version          show the running 3code version
  :sandbox          show the active filesystem sandbox rules
  :sandbox show     (same)
  :sandbox on|off   toggle sandbox enforcement (off = bash unconfined)
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
❯ :model stub-large
===== 844 =====
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
❯
===== 321 =====
  :sandbox allow T  add a writable/connectable rule (+ T)
  :sandbox readonly P  add a read-only rule (* P)
  :sandbox deny T   add a deny rule (- T)
  (T is a path or a host; P a path. Policy syntax: + allow, - deny,
  * read-only; see .3code/sandbox)
  :q :quit          exit (also Ctrl-D)

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
❯ :reasoning high
===== 662 =====

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
❯
===== 691 =====

input:
  single-line   just type and press Enter
  multi-line    Shift+Enter (or Alt+Enter) inserts a newline; Enter submits
  arrows        full cursor navigation across lines and visual wraps
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
❯ :provider alt
===== 195 =====
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
❯
===== 648 =====
  ctrl+arrow    word-by-word jumps (also crosses logical lines)
  home / end    jump to start / end of the current logical line
  ctrl+u        clear the buffer
  ctrl+w        delete the word before the cursor
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
❯ :tokens
===== 223 =====
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
❯
===== 376 =====
  up / down     visual-row up/down inside the buffer; on the top/bottom row recalls history
  tab           complete :commands, provider names, model names
  ctrl+l        clear the screen
  @path         inline file contents (e.g. @src/foo.nim)
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
❯ :clear
===== 271 =====
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
❯
===== 703 =====
           
❯ :provider

stub  [stub-model]
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
❯ :sessions
===== 731 =====
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
❯
===== 753 =====
alt
        
❯ :model

stub-model  [stub]
stub-large
            
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
❯ :sessions all
===== 589 =====
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
❯
===== 580 =====
❯ :reasoning

reasoning: (none)
experimental: level is free-form, type any value
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
❯ :log
===== 918 =====
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
❯
===== 369 =====
                   
❯ :model stub-large

provider  stub
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
❯ :show
===== 421 =====
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
       
❯ :show

no tool calls yet
❯
===== 501 =====
model     stub-large
                 
❯ :reasoning high

provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
       
❯ :show

no tool calls yet
❯ :compact
===== 747 =====
provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
       
❯ :show

no tool calls yet
          
❯ :compact

unknown command: :compact  (try :help)
❯
===== 629 =====
provider  stub
model     stub-large
reasoning high
               
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
       
❯ :show

no tool calls yet
          
❯ :compact

unknown command: :compact  (try :help)
❯ :summarize
===== 257 =====
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
       
❯ :show

no tool calls yet
          
❯ :compact

unknown command: :compact  (try :help)
            
❯ :summarize

failed or not worth it
❯
===== 534 =====
❯ :provider alt

provider  alt
model     alt-model
         
❯ :tokens

no tokens used yet
        
❯ :clear

════════════════════════════════════════
           
❯ :sessions

no saved sessions for this directory
               
❯ :sessions all

no saved sessions for this directory


listing is scoped to this directory — run from ~/data/3code/sessions for all
      
❯ :log

no tool calls yet
       
❯ :show

no tool calls yet
          
❯ :compact

unknown command: :compact  (try :help)
            
❯ :summarize

failed or not worth it
❯ :prompt
===== 132 =====
# Risk

Act freely on local, reversible work. Pause and explain before: destructive actions (`rm -rf` outside cwd, dropping tables), har
d-to-reverse actions (force-push, amending published commits, removing deps), or anything externally visible (pushing code, open
ing PRs, sending email). When in doubt, ask.

# Git

Prefer new commits over amending. Never skip hooks unless explicitly asked. Stage specific files; avoid `git add -A`. Don't push
 or commit unless asked.

# Security

Don't write code with command injection, XSS, SQL injection, path traversal, or unescaped shell-outs of user input. Don't disabl
e TLS verification. If you spot something insecure, fix it immediately.

# Web research

Use `web_search` to locate sources, then `web_fetch` to read them. Don't paraphrase a snippet as if you'd read the page — fetch
it. Prefer primary sources (official docs, spec, repo) over aggregators. Two independent sources before claiming a fact; mark si
ngle-source claims. Date-check fast-moving topics. Don't invent URLs. Cap at ~5 fetches per question. If searches don't turn up
a clear answer, say so — don't guess.

# Skills

Before using unfamiliar tools, `cat` a matching skill file from the list below.

Available:
- <tty-run>/data/3code/skills/cybernetic-plan.md
- <tty-run>/data/3code/skills/role-conversational.md
- <tty-run>/data/3code/skills/role-sysadmin.md
- <tty-run>/data/3code/skills/role-thinking-partner.md
- <tty-run>/data/3code/skills/role-writing.md
- <tty-run>/data/3code/skills/task-debug-systematic.md

# Tone

Brief. State results, not deliberation. Match response shape to task. End-of-turn: one sentence on what changed, one on what's n
ext. No emoji, no forced cheer. Code refs as `path:line`. If the task was already done, say so and stop.
❯
===== 558 =====
# Risk

Act freely on local, reversible work. Pause and explain before: destructive actions (`rm -rf` outside cwd, dropping tables), har
d-to-reverse actions (force-push, amending published commits, removing deps), or anything externally visible (pushing code, open
ing PRs, sending email). When in doubt, ask.

# Git

Prefer new commits over amending. Never skip hooks unless explicitly asked. Stage specific files; avoid `git add -A`. Don't push
 or commit unless asked.

# Security

Don't write code with command injection, XSS, SQL injection, path traversal, or unescaped shell-outs of user input. Don't disabl
e TLS verification. If you spot something insecure, fix it immediately.

# Web research

Use `web_search` to locate sources, then `web_fetch` to read them. Don't paraphrase a snippet as if you'd read the page — fetch
it. Prefer primary sources (official docs, spec, repo) over aggregators. Two independent sources before claiming a fact; mark si
ngle-source claims. Date-check fast-moving topics. Don't invent URLs. Cap at ~5 fetches per question. If searches don't turn up
a clear answer, say so — don't guess.

# Skills

Before using unfamiliar tools, `cat` a matching skill file from the list below.

Available:
- <tty-run>/data/3code/skills/cybernetic-plan.md
- <tty-run>/data/3code/skills/role-conversational.md
- <tty-run>/data/3code/skills/role-sysadmin.md
- <tty-run>/data/3code/skills/role-thinking-partner.md
- <tty-run>/data/3code/skills/role-writing.md
- <tty-run>/data/3code/skills/task-debug-systematic.md

# Tone

Brief. State results, not deliberation. Match response shape to task. End-of-turn: one sentence on what changed, one on what's n
ext. No emoji, no forced cheer. Code refs as `path:line`. If the task was already done, say so and stop.
❯ :toknes
===== 918 =====
ing PRs, sending email). When in doubt, ask.

# Git

Prefer new commits over amending. Never skip hooks unless explicitly asked. Stage specific files; avoid `git add -A`. Don't push
 or commit unless asked.

# Security

Don't write code with command injection, XSS, SQL injection, path traversal, or unescaped shell-outs of user input. Don't disabl
e TLS verification. If you spot something insecure, fix it immediately.

# Web research

Use `web_search` to locate sources, then `web_fetch` to read them. Don't paraphrase a snippet as if you'd read the page — fetch
it. Prefer primary sources (official docs, spec, repo) over aggregators. Two independent sources before claiming a fact; mark si
ngle-source claims. Date-check fast-moving topics. Don't invent URLs. Cap at ~5 fetches per question. If searches don't turn up
a clear answer, say so — don't guess.

# Skills

Before using unfamiliar tools, `cat` a matching skill file from the list below.

Available:
- <tty-run>/data/3code/skills/cybernetic-plan.md
- <tty-run>/data/3code/skills/role-conversational.md
- <tty-run>/data/3code/skills/role-sysadmin.md
- <tty-run>/data/3code/skills/role-thinking-partner.md
- <tty-run>/data/3code/skills/role-writing.md
- <tty-run>/data/3code/skills/task-debug-systematic.md

# Tone

Brief. State results, not deliberation. Match response shape to task. End-of-turn: one sentence on what changed, one on what's n
ext. No emoji, no forced cheer. Code refs as `path:line`. If the task was already done, say so and stop.
         
❯ :toknes

unknown command: :toknes  did you mean :tokens?
❯
===== 489 =====
ing PRs, sending email). When in doubt, ask.

# Git

Prefer new commits over amending. Never skip hooks unless explicitly asked. Stage specific files; avoid `git add -A`. Don't push
 or commit unless asked.

# Security

Don't write code with command injection, XSS, SQL injection, path traversal, or unescaped shell-outs of user input. Don't disabl
e TLS verification. If you spot something insecure, fix it immediately.

# Web research

Use `web_search` to locate sources, then `web_fetch` to read them. Don't paraphrase a snippet as if you'd read the page — fetch
it. Prefer primary sources (official docs, spec, repo) over aggregators. Two independent sources before claiming a fact; mark si
ngle-source claims. Date-check fast-moving topics. Don't invent URLs. Cap at ~5 fetches per question. If searches don't turn up
a clear answer, say so — don't guess.

# Skills

Before using unfamiliar tools, `cat` a matching skill file from the list below.

Available:
- <tty-run>/data/3code/skills/cybernetic-plan.md
- <tty-run>/data/3code/skills/role-conversational.md
- <tty-run>/data/3code/skills/role-sysadmin.md
- <tty-run>/data/3code/skills/role-thinking-partner.md
- <tty-run>/data/3code/skills/role-writing.md
- <tty-run>/data/3code/skills/task-debug-systematic.md

# Tone

Brief. State results, not deliberation. Match response shape to task. End-of-turn: one sentence on what changed, one on what's n
ext. No emoji, no forced cheer. Code refs as `path:line`. If the task was already done, say so and stop.
         
❯ :toknes

unknown command: :toknes  did you mean :tokens?
❯ :toknes
